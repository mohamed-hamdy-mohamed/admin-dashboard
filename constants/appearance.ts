import { LanguageOption, ThemeOption } from "@/types/settings";

export const THEME_OPTIONS: ThemeOption[] = ["light", "dark", "system"];

export const LANGUAGE_OPTIONS: LanguageOption[] = ["en", "ar"];
